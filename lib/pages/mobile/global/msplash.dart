import 'package:flutter/material.dart';

class MobileSplashPage extends StatelessWidget {
  
  const MobileSplashPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'DaDev'
        ),
      ),
    );
  }

}